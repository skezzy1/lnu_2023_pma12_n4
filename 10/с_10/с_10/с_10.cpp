#include<iostream>
using namespace std;

int main()
{
    int n;
    cin >> n;
    cout << "Enter a number: ";
    for (int i = 0; i < n; i++)
    {
        if (n > n = pow(10,6)) {
            cout << "Limit is over";
        }else if(n == 0) {
            cout << "Null is not natural number";
        }else {
            cout << "Correct result" << n << endl;
        }
    }
    return 0;
}