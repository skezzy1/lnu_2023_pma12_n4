#include <iostream>
#include <cmath>
using namespace std;

int main() {
	float a, b, h, y;
    cout << "Enter a, b and h: ";
    cin >> a >> b >> h;
    float i = a;
    while(i <= b){
    	y = pow(x, 2) + pow((exp),(-x));
        cout << "x = " << x << ";\t y = " << y < <<endl;
	}
	x+=h;
	return 0;
}
